# ── AI / assistant tooling (keep private) ─────────────────────────────
.claude/
.claude
CLAUDE.md
CLAUDE.local.md
.cursor/
.cursorrules
.aider*
.continue/
.github/copilot-instructions.md
.mcp.json
.windsurfrules

# ── Dependencies ──────────────────────────────────────────────────────
node_modules/

# ── Cloudflare Worker: SECRETS + KEY INVENTORY (never commit — repo is public)
#    The Legend keys are the product. They live only locally + in D1.
worker/data/*.csv
worker/migrations/0002_seed_legend_keys.sql
worker/.dev.vars
worker/.wrangler/
.wrangler/

# ── Logs / temp ───────────────────────────────────────────────────────
*.log
npm-debug.log*
.cache/
tmp/
*.tmp

# ── OS / editor cruft ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
