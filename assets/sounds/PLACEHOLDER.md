# MOUSSY — Sound Assets
# Place premium audio clips here.
# Supported formats: .mp3, .ogg (ogg recommended for smaller file sizes)
#
# Expected clips (names are referenced by offscreen/offscreen.js):
#   gesture_match.ogg     — played when a gesture is successfully recognised
#   gesture_trail.ogg     — soft ambient tick during gesture drawing (optional)
#   gesture_fail.ogg      — subtle error tone for unrecognised gesture
#   gesture_action.ogg    — confirmation sound when an action is executed
